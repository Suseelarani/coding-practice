#include <stdio.h>

int main() {
	// your code goes here
    int t,x;
    scanf("%d",&t);
    while(t--) {
        scanf("%d",&x);
        if(x<=10) {
            printf("Yes\n");
        }
        else {
            printf("No\n");
        }
    }
    return 0;
}
